#pragma once

enum class SkillType
{
    RegularAttack, // non skill attack, using weapon for party
    Attack,
    Heal,
    Buff,
    Debuff,
    MultiAttack,
    MultiHeal,
    MultiBuff,
    MultiDebuff,
    AllOutAttack,
};
